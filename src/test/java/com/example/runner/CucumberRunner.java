package com.example.runner;

import io.cucumber.junit.platform.engine.Cucumber;

@Cucumber
public class CucumberRunner {
    // Uses JUnit Platform to run all features in classpath: src/test/resources/features
}
